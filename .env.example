# GitHub Personal Access Token (로컬 개발용)
# Fine-grained PAT 발급 시 public_repo 읽기 권한만 부여
# 발급: https://github.com/settings/tokens?type=beta
# 본 파일(.env.example)은 커밋 가능, 실제 토큰이 든 .env는 .gitignore에 의해 커밋 차단됨
GITHUB_TOKEN=
